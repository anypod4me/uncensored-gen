// next.config.mjs
const nextConfig = {
  reactStrictMode: true, // Enforce React strict mode
  compiler: {
    styledComponents: true, // Example for styled-component support
  },
};

export default nextConfig;